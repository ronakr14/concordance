[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    unit: fast, no I/O beyond temp files
    integration: touches the filesystem or a database
    e2e: end-to-end through the CLI
    slow: takes more than a few seconds
    perf: performance budget, opt-in with CONCORDANCE_PERF_TESTS=1
